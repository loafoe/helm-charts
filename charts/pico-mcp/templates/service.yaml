apiVersion: v1
kind: Service
metadata:
  name: {{ include "pico-mcp.fullname" . }}
  labels:
    {{- include "pico-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pico-mcp.selectorLabels" . | nindent 4 }}
