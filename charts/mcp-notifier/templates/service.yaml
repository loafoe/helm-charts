apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-notifier.fullname" . }}
  labels:
    {{- include "mcp-notifier.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-notifier.selectorLabels" . | nindent 4 }}
