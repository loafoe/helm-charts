apiVersion: v2
name: mt-mcp-grafana
description: A multi-tenant proxy that aggregates and secures multiple mcp-grafana backend instances speaking the MCP streamable-HTTP transport.
type: application
version: 0.8.0
appVersion: "0.1.0"
